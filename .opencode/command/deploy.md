---
description: Build the site and deploy to Bunny Storage.
---

Build the Astro site and deploy to production.

1. Run `npm run build` to build the site
2. Run `npm run deploy:sync` to sync the built files to Bunny Storage

Report back the build output and deployment status.
